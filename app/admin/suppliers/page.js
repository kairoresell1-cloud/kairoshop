"use client";
import AdminPageShell from "@/components/AdminPageShell";
export default function Page() {
  return <AdminPageShell title="Fornitori (ERP)" description="Costi, lotti, margini — riservato a Owner." requireOwner />;
}
